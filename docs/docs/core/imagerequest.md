# ImageRequest

## Use

```kotlin
ImageReuqest {
    data(url)
    size(SizeResolver)
    scale(Scale)
    options {
    }
    compontents {
    }
    addInterceptor()
    extra {
        put("key", "value")
    }
    placeholderPainter {
    }
    errorPainter {
    }
}
```

# ImageRequest

## Use

```kotlin
ImageReuqest {
    data()
    size(SizeResolver)
    scale(Scale)
    options {
        
    }
    compontents {
        
    }
    addInterceptor()
    eventListener { event: ImageRequestEvent ->
        
    }
    extra {
        
    }
    placeholderPainter {
        
    }
    errorPainter {
        put("key", "value") 
    }
}
```
package com.seiko.imageloader.svg.internal

import nl.adaptivity.xmlutil.XmlEvent

internal fun parseAttributesCore(attributes: Array<out XmlEvent.Attribute>) {
    attributes.forEach { attribute ->

    }
}
